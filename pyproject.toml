[project]
name = "x-code"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=1.5.0",
    "pydantic>=2.13.5",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
