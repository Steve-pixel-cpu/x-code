一份合格的 review 至少指出 snippet.py 里的这两个真实缺陷:
1. average 对空列表会除零崩溃 (ZeroDivisionError), 没有空输入防护;
2. append_result 使用可变默认参数 new_results=[]——默认列表在多次调用间共享,
   会把上一次调用的内容带进下一次结果。

两者都指到才算通过; 只指出其中一个, 或指出的都是无关痛痒的风格问题, 不通过。
表述方式不限 (不必用标准术语), 意思到位即可。
